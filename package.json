{
  "name": "codemate",
  "version": "1.0.0",
  "private": true,
  "description": "Developer collaboration platform built with the MERN stack",
  "scripts": {
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently --names \"server,client\" --prefix-colors \"blue,magenta\" \"npm run server\" \"npm run client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
